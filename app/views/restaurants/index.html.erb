<div class="container m-3">
  <h1>Restaurants</h1>
  <%= link_to 'Add a new restaurant', new_restaurant_path, class: 'btn btn-outline-secondary'%>
  <div class="cards">
    <% @restaurants.each do |restaurant| %>
      <%= render 'restaurants/card', restaurant: restaurant %>
    <% end %>
  </div>
</div>
