<div class="container m-3">
  <h1>Add review</h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: Review::SPECIES.sort, include_blank: false %>
    <%= f.submit class: 'btn btn-primary mt-2' %>
  <% end %>
</div>
